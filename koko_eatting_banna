def minEatingSpeed(piles: List[int], h: int) -> int:
    low = 1
    high = max(piles)

    while low < high:
        k = (low + high)//2
        time = 0
        for pile in piles:
            time += (pile + k - 1)//k
        if time > h:
            low = k + 1
        elif time <= h:
            high = k    
    
    return high

piles = [1,4,3,2]

h = 9

print(minEatingSpeed(piles, h))