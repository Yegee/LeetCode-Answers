def twoSum(nums, target) :

    hash = {}
    
    for i in range(len(nums)):
        sum = target - nums[i]

        if sum in hash:
            return [hash[sum], i]
        else:
            hash[nums[i]] = i

    return [0,0]


nums = [3,4,5,6]
target = 7

print(twoSum(nums, target))