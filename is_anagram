def isAnagram(self, s: str, t: str) -> bool:
    s_hash = {}
    t_hash = {}

    if len(s) != len(t):
            return False

    for i in s:
        s_hash.setdefault(i, 0)
        s_hash[i] += 1
    
    for i in t:
        t_hash.setdefault(i, 0)
        t_hash[i] += 1
        
    return s_hash == t_hash