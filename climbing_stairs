def climbStairs(n):
        """
        :type n: int
        :rtype: int
        """
        steps = [1,1]
        if n == 1 or n == 2:
            return n
        for stair in range(2,n):
            steps.append(steps[stair - 2] + steps[stair - 1])
        
        return steps[-1]
            

        

print(climbStairs(45))